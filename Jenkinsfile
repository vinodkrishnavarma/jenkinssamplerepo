pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh '''
                ls -al
                pwd
                echo "Hello-World Jenkins"
                '''
                
            }
        }
    }
}
